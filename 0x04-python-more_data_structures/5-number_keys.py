#!/usr/bin/python3

def number_keys(a_dictionary):
    """should return the number of keys in a dictionary
    You are not allowed to import any module
    """

    # this is an alernative
    # keys = list(a_dictionary.keys())
    # count = 0
    # for count in keys:
    #     count += 1
    # return count

    return len(a_dictionary)
